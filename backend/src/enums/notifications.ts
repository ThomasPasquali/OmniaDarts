export enum NotificationState {
  NEW = 'NEW',
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  REJECTED = 'REJECTED',
  IGNORED = 'IGNORED',
}

export enum NotificationAction {
  ACCEPT = 'ACCEPT',
  REJECT = 'REJECT',
  IGNORE = 'IGNORE',
}
